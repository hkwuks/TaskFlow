{
  "description": "TaskFlow lifecycle hooks: SessionStart state summary (Codex CLI plugin form).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${PLUGIN_ROOT}/hooks/session-start\""
          }
        ]
      }
    ]
  }
}